function rolar1() {
    var roll = Math.floor(Math.random() * 4 + 1)
    var bloco = document.getElementById('cubo1')
    bloco.innerHTML = ""
    bloco.innerHTML = '<h2>Sua rolagem de dados deu:</h2>'
     if (roll == 1) {
        var image = document.createElement('img')
        image.src = 'dados/1d4.png'
        bloco.appendChild(image)
    } else if (roll == 2) {
        var image = document.createElement('img')
        image.src = 'dados/2d4.png'
        bloco.appendChild(image) 
    } else if (roll == 3) {
        var image = document.createElement('img')
        image.src = 'dados/3d4.png'
        bloco.appendChild(image) 
    } else if (roll == 4) {
        var image = document.createElement('img')
        image.src = 'dados/4d4.png'
        bloco.appendChild(image) 
    }
}

function rolar2() {
    var roll = Math.floor(Math.random() * 6 + 1)
    var bloco = document.getElementById('cubo1')
    bloco.innerHTML = ""
    bloco.innerHTML = '<h2>Sua rolagem de dados deu:</h2>'
    if (roll == 1) {
        var image = document.createElement('img')
        image.src = 'dados/1d6.png'
        bloco.appendChild(image)
    } else if (roll == 2) {
        var image = document.createElement('img')
        image.src = 'dados/2d6.png'
        bloco.appendChild(image) 
    } else if (roll == 3) {
        var image = document.createElement('img')
        image.src = 'dados/3d6.png'
        bloco.appendChild(image) 
    } else if (roll == 4) {
        var image = document.createElement('img')
        image.src = 'dados/4d6.png'
        bloco.appendChild(image) 
    } else if (roll == 5) {
        var image = document.createElement('img')
        image.src = 'dados/5d6.png'
        bloco.appendChild(image) 
    } else if (roll == 6) {
        var image = document.createElement('img')
        image.src = 'dados/6d6.png'
        bloco.appendChild(image) 
    }
}

function rolar3() {
    var roll = Math.floor(Math.random() * 8 + 1)
    var bloco = document.getElementById('cubo1')
    bloco.innerHTML = ""
    bloco.innerHTML = '<h2>Sua rolagem de dados deu:</h2>'
    if (roll == 1) {
        var image = document.createElement('img')
        image.src = 'dados/1d8.png'
        bloco.appendChild(image)
    } else if (roll == 2) {
        var image = document.createElement('img')
        image.src = 'dados/2d8.png'
        bloco.appendChild(image) 
    } else if (roll == 3) {
        var image = document.createElement('img')
        image.src = 'dados/3d8.png'
        bloco.appendChild(image) 
    } else if (roll == 4) {
        var image = document.createElement('img')
        image.src = 'dados/4d8.png'
        bloco.appendChild(image) 
    } else if (roll == 5) {
        var image = document.createElement('img')
        image.src = 'dados/5d8.png'
        bloco.appendChild(image) 
    } else if (roll == 6) {
        var image = document.createElement('img')
        image.src = 'dados/6d8.png'
        bloco.appendChild(image) 
    } else if (roll == 7) {
        var image = document.createElement('img')
        image.src = 'dados/7d8.png'
        bloco.appendChild(image) 
    } else if (roll == 8) {
        var image = document.createElement('img')
        image.src = 'dados/8d8.png'
        bloco.appendChild(image) 
    }
}

function rolar4() {
    var roll = Math.floor(Math.random() * 10 + 1)
    var bloco = document.getElementById('cubo1')
    bloco.innerHTML = ""
    bloco.innerHTML = '<h2>Sua rolagem de dados deu:</h2>'
    if (roll == 1) {
        var image = document.createElement('img')
        image.src = 'dados/1d10.png'
        bloco.appendChild(image)
    } else if (roll == 2) {
        var image = document.createElement('img')
        image.src = 'dados/2d10.png'
        bloco.appendChild(image) 
    } else if (roll == 3) {
        var image = document.createElement('img')
        image.src = 'dados/3d10.png'
        bloco.appendChild(image) 
    } else if (roll == 4) {
        var image = document.createElement('img')
        image.src = 'dados/4d10.png'
        bloco.appendChild(image) 
    } else if (roll == 5) {
        var image = document.createElement('img')
        image.src = 'dados/5d10.png'
        bloco.appendChild(image) 
    } else if (roll == 6) {
        var image = docoment.createElement('img')
        image.src = 'dados/6d10.png'
        bloco.appendChild(image) 
    } else if (roll == 7) {
        var image = document.createElement('img')
        image.src = 'dados/7d10.png'
        bloco.appendChild(image) 
    } else if (roll == 8) {
        var image = document.createElement('img')
        image.src = 'dados/8d10.png'
        bloco.appendChild(image) 
    } else if (roll == 9) {
        var image = document.createElement('img')
        image.src = 'dados/9d10.png'
        bloco.appendChild(image) 
    } else if (roll == 10) {
        var image = document.createElement('img')
        image.src = 'dados/10d10.png'
        bloco.appendChild(image) 
    }
}

function rolar5() {
    var roll = Math.floor(Math.random() * 12 + 1)
    var bloco = document.getElementById('cubo1')
    bloco.innerHTML = ""
    bloco.innerHTML = '<h2>Sua rolagem de dados deu:</h2>'
    if (roll == 1) {
        var image = document.createElement('img')
        image.src = 'dados/1d12.png'
        bloco.appendChild(image)
    } else if (roll == 2) {
        var image = document.createElement('img')
        image.src = 'dados/2d12.png'
        bloco.appendChild(image) 
    } else if (roll == 3) {
        var image = document.createElement('img')
        image.src = 'dados/3d12.png'
        bloco.appendChild(image) 
    } else if (roll == 4) {
        var image = document.createElement('img')
        image.src = 'dados/4d12.png'
        bloco.appendChild(image) 
    } else if (roll == 5) {
        var image = document.createElement('img')
        image.src = 'dados/5d12.png'
        bloco.appendChild(image) 
    } else if (roll == 6) {
        var image = document.createElement('img')
        image.src = 'dados/6d12.png'
        bloco.appendChild(image) 
    } else if (roll == 7) {
        var image = document.createElement('img')
        image.src = 'dados/7d12.png'
        bloco.appendChild(image) 
    } else if (roll == 8) {
        var image = document.createElement('img')
        image.src = 'dados/8d12.png'
        bloco.appendChild(image) 
    } else if (roll == 9) {
        var image = document.createElement('img')
        image.src = 'dados/9d12.png'
        bloco.appendChild(image) 
    } else if (roll == 10) {
        var image = document.createElement('img')
        image.src = 'dados/10d12.png'
        bloco.appendChild(image) 
    } else if (roll == 11) {
        var image = document.createElement('img')
        image.src = 'dados/dados/11d12.png'
        bloco.appendChild(image) 
    } else if (roll == 12) {
        var image = document.createElement('img')
        image.src = 'dados/12d12.png'
        bloco.appendChild(image) 
    }
}

function rolar6() {
    var roll = Math.floor(Math.random() * 20 + 1)
    var bloco = document.getElementById('cubo1')
    bloco.innerHTML = ""
    bloco.innerHTML = '<h2>Sua rolagem de dados deu:</h2>'
    if (roll == 1) {
        var image = document.createElement('img')
        image.src = 'dados/1d20.png'
        bloco.appendChild(image)
    } else if (roll == 2) {
        var image = document.createElement('img')
        image.src = 'dados/2d20.png'
        bloco.appendChild(image) 
    } else if (roll == 3) {
        var image = document.createElement('img')
        image.src = 'dados/3d20.png'
        bloco.appendChild(image) 
    } else if (roll == 4) {
        var image = document.createElement('img')
        image.src = 'dados/4d20.png'
        bloco.appendChild(image) 
    } else if (roll == 5) {
        var image = document.createElement('img')
        image.src = 'dados/5d20.png'
        bloco.appendChild(image) 
    } else if (roll == 6) {
        var image = document.createElement('img')
        image.src = 'dados/6d20.png'
        bloco.appendChild(image) 
    } else if (roll == 7) {
        var image = document.createElement('img')
        image.src = 'dados/7d20.png'
        bloco.appendChild(image) 
    } else if (roll == 8) {
        var image = document.createElement('img')
        image.src = 'dados/8d20.png'
        bloco.appendChild(image) 
    } else if (roll == 9) {
        var image = document.createElement('img')
        image.src = 'dados/9d20.png'
        bloco.appendChild(image) 
    } else if (roll == 10) {
        var image = document.createElement('img')
        image.src = 'dados/10d20.png'
        bloco.appendChild(image) 
    } else if (roll == 11) {
        var image = document.createElement('img')
        image.src = 'dados/11d20.png'
        bloco.appendChild(image) 
    } else if (roll == 12) {
        var image = document.createElement('img')
        image.src = 'dados/12d20.png'
        bloco.appendChild(image) 
    } else if (roll == 13) {
        var image = document.createElement('img')
        image.src = 'dados/13d20.png'
        bloco.appendChild(image) 
    } else if (roll == 14) {
        var image = document.createElement('img')
        image.src = 'dados/14d20.png'
        bloco.appendChild(image) 
    } else if (roll == 15) {
        var image = document.createElement('img')
        image.src = 'dados/15d20.png'
        bloco.appendChild(image) 
    } else if (roll == 16) {
        var image = document.createElement('img')
        image.src = 'dados/16d20.png'
        bloco.appendChild(image) 
    } else if (roll == 17) {
        var image = document.createElement('img')
        image.src = 'dados/17d20.png'
        bloco.appendChild(image) 
    } else if (roll == 18) {
        var image = document.createElement('img')
        image.src = 'dados/18d20.png'
        bloco.appendChild(image) 
    } else if (roll == 19) {
        var image = document.createElement('img')
        image.src = 'dados/19d20.png'
        bloco.appendChild(image) 
    } else if (roll == 20) {
        var image = document.createElement('img')
        image.src = 'dados/20d20.png'
        bloco.appendChild(image) 
    }
}
function back() {
    window.location.reload(true)
}